---
name: xhs
description: 小红书自动登录发布工具 - 支持图文和长文发布
required_permissions:
  - browser
  - shell
---

# 小红书自动登录发布 Skill

## 功能概述

使用真实的 Chromium 浏览器模拟人工操作，完成小红书的登录和内容发布。

## 使用方式

```
/xhs
```

或描述你的需求，例如：
- "帮我发布一篇小红书，主题是..."
- "我想写一篇关于xxx的小红书"

## 实现细节

### 1. 浏览器启动

使用 Playwright 启动 Chromium 浏览器：

```
chromium --no-first-run --disable-dev-shm-usage
```

启动参数说明：
- `--no-first-run`: 跳过首次运行设置向导
- `--disable-dev-shm-usage`: 避免共享内存问题

### 2. 登录流程

1. 打开 https://www.xiaohongshu.com
2. 检测是否需要登录（检查登录按钮是否存在）
3. 如果需要登录：
   - 等待用户扫码或手动输入账号密码
   - 等待登录成功
   - 保存登录状态（cookies/localStorage）
4. 保持浏览器会话打开，不关闭

### 3. 内容创作流程

#### 图文发布

1. 点击「发布」按钮 → 新标签页打开创作服务平台
2. 点击「图文」上传
3. 点击「文字配图」
4. 在 `editor-paragraph` 元素中输入正文内容
5. 点击「生成图片」按钮
6. 在「选择一个喜欢的卡片」中随机选择一个
7. 点击「下一步」
8. 填写发布表单（标题、话题等）
9. 点击「发布」

#### 长文发布

1. 点击「发布」按钮 → 新标签页打开创作服务平台
2. 点击「新的创作」
3. 输入标题和正文
4. 点击「一键排版」
5. 依次点击「下一步」
6. 填写发布表单
7. 点击「发布」

### 4. 人性化操作模拟

- 随机延迟模拟人类操作节奏
- 使用人类的点击模式（非精确坐标点击）
- 渐进式滚动避免被检测

### 5. 会话保持

- 保存浏览器 profile 到本地
- 下次运行时恢复会话
- 检查登录状态，如过期重新登录

## 代码结构

```javascript
// 主要模块
- initBrowser()      // 初始化浏览器
- checkLogin()       // 检查登录状态
- doLogin()          // 执行登录
- saveSession()      // 保存会话
- loadSession()      // 加载会话
- generateContent()  // 生成内容
- publishImageText() // 发布图文
- publishLongForm()  // 发布长文
```

## 测试步骤

1. 运行 `/xhs`
2. 首次会提示登录，扫码登录
3. 确认登录成功后，描述你想发布的内容
4. 选择图文或长文模式
5. 等待自动发布完成

---

**注意**：请确保 Chromium 已安装在你的系统中。
